{
  "name": "@vinext-laravel-starter/http-contract",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsp compile . && bun run generate:operations",
    "generate:operations": "bun run generate-operations.mjs",
    "test": "bun test generate-operations.test.mjs"
  },
  "devDependencies": {
    "@typespec/compiler": "1.15.0",
    "@typespec/http": "1.15.0",
    "@typespec/openapi": "1.15.0",
    "@typespec/openapi3": "1.15.0",
    "yaml": "2.9.0"
  }
}
